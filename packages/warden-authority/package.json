{
  "name": "@believers-common/warden-authority",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-neutral Warden issuance and policy evaluation for governed Agent authority.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build-check": "tsc --noEmit -p ./tsconfig.test.json",
    "test": "vitest run test/issuance.test.ts"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "files": [
    "dist"
  ]
}
